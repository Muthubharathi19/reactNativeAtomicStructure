import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
} from 'react-native';
import InputMolecule from '../molecules/InputMolecule';
const InputOrganism = () => {
  return (
    <SafeAreaView>
     <InputMolecule />
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
    
});

export default InputOrganism;

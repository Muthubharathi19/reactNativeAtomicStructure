import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  Text
} from 'react-native';

const LabelAtom = () => {
  return (
    <SafeAreaView>
      <Text>Input Label</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  
});

export default LabelAtom;
